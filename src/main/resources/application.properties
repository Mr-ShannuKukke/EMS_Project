spring.application.name=EMS_Project

spring.datasource.url=jdbc:mysql://localhost:3306/ems_dto?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Shannu@sql123

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
#Above line is not required. If used, your application will get crashed with below error:
#Unable to load class: org.hibernate.dialect.MySQL8Dialect
#
#This happens because Hibernate ORM version 7 (used by Spring Boot 4) changed the dialect name.

#Remove above line or change it to below:
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

server.port=8081